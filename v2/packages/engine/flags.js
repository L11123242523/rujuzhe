/**
 * 入局者 v2 · flag 生存期登记（历史入口，保留兼容）
 * ---------------------------------------------------------------------------
 * 声明本体已移到 **state.js**（任何创建 state 的入口都必然 import 它，
 * 避免"忘了 import 声明表 ⇒ 所有 flag 判未声明"的隐式耦合）。
 * 本文件只为既有 import 保留一个稳定的转发点。
 */

export { ENGINE_FLAGS } from './state.js';
